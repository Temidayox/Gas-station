datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}
```

**2. Add `DIRECT_URL` to your `.env.local`:**
```
DIRECT_URL="postgresql://postgres.fsastsbfpruynhsnmvul:[MRyWX!6JpC-dG.3]@aws-1-eu-west-1.pooler.supabase.com:5432/postgres"